div(ng-controller="Notifications" ng-init="notificationsList=#{JSON.stringify(data)}")
	br
	if data.length == 0
		div(class="alert alert-info" style="margin-bottom:0px;")
			button(type="button" class="close" data-dismiss="alert" ng-click="closeAlertInfo($event)")&times;
			| Vous n'avez reçu aucune nouvelle notification.
	else
		div(class="alert alert-info" style="margin-bottom:0px; margin-top:5px;" ng-repeat="notif in notificationsList")
			item {{notif.pseudo}} souhaite vous faire découvrir {{notif.musicName}} de {{notif.author}} depuis l'url {{notif.url}}
			button(type="button" class="" ng-click="play(notif)") Listen
			&nbsp;
			button(type="button" class="" ng-click="addToCurrentPlaylist(notif, playlist_selected)") Add to current playlist