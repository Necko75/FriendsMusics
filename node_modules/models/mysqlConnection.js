// DB //
var mysql = require('mysql');

var DB = mysql.createConnection(
    {
      host     : 'localhost',
      user     : 'root',
      password : '',
      database : 'FM',	
    }
);

DB.connect();
module.exports = DB;